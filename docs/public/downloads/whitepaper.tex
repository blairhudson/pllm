\documentclass[10pt,letterpaper]{article}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage[letterpaper,top=.46in,bottom=.48in,left=.56in,right=.56in]{geometry}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,calc,positioning}
\usepackage{multicol}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{booktabs,tabularx,colortbl}
\usepackage[hidelinks]{hyperref}
\hypersetup{
  pdftitle={Private inference for an open compute world},
  pdfauthor={Blair Hudson},
  pdfsubject={PLLM public-weight prepared inference},
  pdfkeywords={private inference, prepared inventory, non-collusion}
}

\definecolor{pllmInk}{HTML}{112123}
\definecolor{pllmGreen}{HTML}{467A35}
\definecolor{pllmPale}{HTML}{E8EFE4}
\definecolor{pllmWarm}{HTML}{F2EBDD}
\definecolor{pllmLine}{HTML}{AAB9AC}
\definecolor{pllmMuted}{HTML}{506462}

\renewcommand{\familydefault}{\sfdefault}
\color{pllmInk}
\setlength{\parindent}{0pt}
\setlength{\parskip}{3.2pt}
\setlength{\columnsep}{.28in}
\setlist{nosep,leftmargin=1.2em,labelsep=.45em}
\setlist[itemize]{label=\textcolor{pllmGreen}{\rule{.38em}{.38em}}}
\setlist[enumerate]{label=\textcolor{pllmGreen}{\bfseries\arabic*.}}
\titleformat{\section}{\fontsize{12.4}{13.2}\selectfont\bfseries\color{pllmInk}}{}{0pt}{}
\titlespacing*{\section}{0pt}{7pt}{2pt}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[L]{\fontsize{7}{8}\selectfont\color{pllmMuted} PLLM WHITEPAPER}
\fancyfoot[R]{\fontsize{7}{8}\selectfont\color{pllmMuted} \thepage\ / 2}

\newcommand{\evidence}[1]{\textcolor{pllmGreen}{\bfseries\scriptsize #1}}
\newcommand{\refmark}[1]{\textsuperscript{\textcolor{pllmGreen}{[#1]}}}

\begin{document}
\thispagestyle{fancy}

\begin{minipage}[t]{\textwidth}
{\fontsize{26}{27}\selectfont\bfseries Private inference for\\an open compute world}\\[7pt]
{\fontsize{11}{14}\selectfont\color{pllmMuted} Private inference can let organizations use remote AI compute without exposing prompts, context, or generated output to the infrastructure provider. PLLM keeps sensitive data on the client and sends protected requests to remote services. This design could support a broader market of independent compute without requiring users to trust one provider with all of their data.}
\end{minipage}

\vspace{8pt}\textcolor{pllmLine}{\rule{\textwidth}{.6pt}}\vspace{5pt}

\begin{tikzpicture}[x=1cm,y=1cm,>=Latex,font=\sffamily]
  \tikzset{
    role/.style={draw=pllmLine,rounded corners=3pt,minimum height=1.72cm,align=left,inner sep=9pt},
    flow/.style={draw=pllmGreen,line width=.8pt,-{Latex[length=4pt]}},
    note/.style={font=\fontsize{6.4}{7.2}\selectfont,fill=white,inner sep=2pt,text=pllmMuted,align=center},
    order/.style={circle,fill=pllmInk,text=white,font=\bfseries\fontsize{6}{6}\selectfont,inner sep=0pt,minimum size=12pt}
  }
  \node[role,fill=pllmPale,text width=3.80cm] (client) at (1.95,2.90) {\evidence{01}\quad\textbf{Client}\\[4pt]\fontsize{7.4}{8.4}\selectfont Prompt, masks, state, sampling};
  \node[role,fill=pllmWarm,text width=3.80cm] (prep) at (8.20,2.90) {\evidence{02}\quad\textbf{Trusted preparation}\\[4pt]\fontsize{7.4}{8.4}\selectfont Root seeds and public body; computes $W r-s$};
  \node[role,fill=white,text width=3.80cm] (infer) at (14.45,2.90) {\evidence{03}\quad\textbf{Inference provider}\\[4pt]\fontsize{7.4}{8.4}\selectfont Corrections, tickets and $x-r$};
  \draw[dashed,pllmGreen,line width=.8pt] (11.33,.56) -- (11.33,4.90);
  \draw[flow] (client.east) -- (prep.west);
  \node[note] at (5.48,3.32) {ROOT SEEDS};
  \node[order] at (4.38,3.32) {1};
  \draw[flow] (prep.east) -- (infer.west);
  \node[note] at (11.58,3.32) {$Wr-s$ ROWS};
  \node[order] at (10.48,3.32) {2};
  \coordinate (clientOnline) at ([xshift=-.32cm,yshift=.70cm]client.north);
  \coordinate (inferOnline) at ([xshift=-.32cm,yshift=.70cm]infer.north);
  \draw[flow] ([xshift=-.32cm]client.north) -- (clientOnline) -- (inferOnline) -- ([xshift=-.32cm]infer.north);
  \node[note] at (8.25,4.78) {ONLINE / TICKET + $x-r$};
  \node[order] at (6.18,4.78) {3};
  \coordinate (inferReturn) at ([xshift=.32cm,yshift=-.70cm]infer.south);
  \coordinate (clientReturn) at ([xshift=.32cm,yshift=-.70cm]client.south);
  \draw[flow] ([xshift=.32cm]infer.south) -- (inferReturn) -- (clientReturn) -- ([xshift=.32cm]client.south);
  \node[note] at (8.25,1.02) {$Wx-s$; CLIENT ADDS $s$};
  \node[order] at (6.18,1.02) {4};
  \node[font=\fontsize{6.4}{7}\selectfont\bfseries,text=pllmGreen,fill=white,inner sep=2pt] at (11.33,.56) {SEPARATE OPERATORS};
\end{tikzpicture}

\vspace{1pt}
\begin{multicols}{2}
\section*{What PLLM changes}
Ordinary hosted inference bundles computation with access to prompts and generated text. PLLM instead keeps tokenization, nonlinear operations, attention state, sampling, and decoding in the client environment. Public transformer-body matrices run remotely as masked integer projections. Compute can therefore be selected independently from willingness to disclose client language. This is a protocol boundary, not a URL setting.\refmark{1}

\section*{Prepared inventory}
Before chat, the client commits an inventory to one model, body, stage set, quantization profile, and attempt budget. It sends one domain-separated root seed per stage to preparation. Preparation expands one-time input masks $r$, output masks $s$, and tickets, computes $Wr-s$, then pushes each batch to inference. Inference acknowledges accepted stage batches and seals the inventory \texttt{READY}. Online, it atomically consumes a ticket and returns
\[
W(x-r)+(Wr-s)=Wx-s.
\]
The client adds $s$. Prefill rows share a compact matrix envelope; decode retains one ticket per stage on a persistent connection. Unused reserved rows burn on cancellation or failure. Inventory is memory-only and disappears on restart or idle expiry.\refmark{1}

\columnbreak
\section*{Boundary, not invisibility}
\textbf{Protected from inference:} plaintext prompts and outputs, token identities, root seeds, masks, activation scales, local attention and recurrent state, and sampling choices. Preparation sees seeds and bound stage metadata, but not online masked activations. Inference sees corrections and masked activations, but not seeds.

\textbf{Still exposed:} both services know the public model, stage names, tensor shapes, quantization profile, scheduling, timing, traffic volume, and approximate sequence lengths. Preparation must follow the protocol, erase expanded masks, and not collude with inference. Two services under one untrusted operator do not meet this assumption; authentication and TLS protect channels but do not prove independence, erasure, or correct model execution.\refmark{2}

SHAKE-256 expands each fresh root seed into pseudorandom masks. Under that computational assumption, each masked activation hides its input from inference when viewed alone. The claim assumes protocol-conforming roles, mask non-retention, and non-collusion; Python releases masks after use but verified physical zeroization is not established. Deployment review must therefore name operators and log-access paths, not merely count endpoints.
\end{multicols}

\clearpage
\thispagestyle{fancy}

{\fontsize{20}{22}\selectfont\bfseries A world of compute, without exposing client data.}

\vspace{7pt}
\begin{tikzpicture}[x=1cm,y=1cm,font=\sffamily]
  \tikzset{rung/.style={draw=pllmLine,rounded corners=3pt,minimum height=1.2cm,align=left,inner sep=7pt}}
  \node[rung,fill=pllmGreen,text=white,draw=pllmGreen,text width=3.55cm] at (1.9,.65) {\textbf{01\quad Protect}\\[2pt]\fontsize{7.2}{8.2}\selectfont Keep language and state at the client edge};
  \node[rung,fill=white,text width=3.55cm] at (6.35,.65) {\evidence{02\quad Admit}\\[2pt]\fontsize{7.2}{8.2}\selectfont Open supply to compatible independent capacity};
  \node[rung,fill=white,text width=3.55cm] at (10.8,.65) {\evidence{03\quad Route}\\[2pt]\fontsize{7.2}{8.2}\selectfont Select by price, latency, region, energy and availability};
  \node[rung,fill=white,text width=3.55cm] at (15.25,.65) {\evidence{04\quad Expand}\\[2pt]\fontsize{7.2}{8.2}\selectfont Widen access through broader compute supply};
  \foreach \x in {3.82,8.27,12.72}{\draw[-{Latex[length=4pt]},pllmGreen,line width=.9pt] (\x,.65) -- +(0.55,0);}
\end{tikzpicture}

\vspace{2pt}
\begin{minipage}[t]{.485\textwidth}\vspace{0pt}
\section*{Unlock untrusted capacity}
Hosted inference usually requires the buyer to trust the compute owner with plaintext. Masked execution separates those choices. Regional clouds, renewable projects, sovereign infrastructure, colocation operators, and independent machines could compete without becoming custodians of client language. Participation still requires compatible execution, availability, credentials, and enforceable market rules---but not permission to read the workload.

\section*{Follow available energy}
Solar generation, grid congestion, and idle capacity vary by place and hour. A scheduler could move masked public-model work toward abundant renewable generation and replenish offline inventory where latency matters less. Privacy makes broader routing plausible; it does not make it free. Dependent client exchanges, correction placement, distance, and operator separation remain physical constraints. PLLM does not yet measure an energy saving.

\section*{Price the complete lifecycle}
{\fontsize{7.2}{8.4}\selectfont\arrayrulecolor{pllmLine}
\begin{tabularx}{\linewidth}{@{}>{\bfseries}p{1.55cm}X@{}}
\toprule
Client & Local model work, state, memory and traffic \\
Preparation & Body storage, matrix work, correction egress and erasure \\
Inference & Masked compute, inventory, traffic and tickets \\
Market & Discovery, settlement, verification and operator separation \\
\bottomrule
\end{tabularx}}
Broader supply can create price pressure and wider access to private AI. This is a market mechanism, not a measured savings result.
\end{minipage}\hfill
\begin{minipage}[t]{.485\textwidth}\vspace{0pt}

\section*{No plaintext training feed}
On the current path, Inference receives one-time tickets and $x-r$; Preparation receives seeds and stage commitments. Neither is sent literal plaintext prompts, token IDs, or decoded output. Under protocol compliance, pseudorandom masks, mask non-retention, and non-collusion, the data plane removes the direct plaintext feed a provider could otherwise retain for training. This is a technical barrier rather than only a policy promise, but not a universal no-learning guarantee: metadata remains visible and collusion defeats the split.\refmark{2}

\section*{Current implementation evidence}
Nine warm Qwen2.5-0.5B runs used revision \texttt{277d19f} on an Apple M5, with three repetitions per exact context after one warmup.\refmark{3}

\vspace{2pt}
{\fontsize{7.5}{9}\selectfont\arrayrulecolor{pllmGreen}
\begin{tabularx}{\linewidth}{@{}>{\raggedright\arraybackslash}Xrrr@{}}
\toprule
\rowcolor{pllmPale}Input tokens & Output & TTFT & Client I/O \\
\midrule
30 & 9 & 0.978 s & 63.33 MB \\
63 & 16 & 1.374 s & 126.29 MB \\
255 & 16 & 5.158 s & 432.71 MB \\
\bottomrule
\end{tabularx}}

\vspace{4pt}
All records ended with authoritative completion events, one retained model/body fingerprint pair, zero online Preparation protocol work, and zero plaintext prompt/token-byte audit counters. These are local CPU loopback measurements, not WAN, GPU, energy, price, or quality results.

\section*{Deployment gates}
Preparation must be independently controlled or client-hosted; target-model quality must pass; the full cost ledger must beat explicit ceilings; and credential separation, erasure, restart behavior, and incident response must survive a pilot.

\section*{References}
\fontsize{7.2}{8.5}\selectfont
[1] PLLM, \href{https://pllm.run/research/architecture/}{Private inference}.\par
[2] PLLM, \href{https://pllm.run/docs/security/}{Security boundaries}.\par
[3] PLLM, \href{https://pllm.run/research/reproduce/}{Run your own benchmark}; \href{https://pllm.run/downloads/current-runtime-2026-09-11.json}{retained study data}.

\end{minipage}

\end{document}
